# backend_series_05
Implemented sliding-window rate limiting to control how many client requests are allowed within a time window. Learned how Redis stores request timestamps, manages limits, and prevents abuse. Added secure auth with JWT, Redis logout, and token blacklisting.
